version: '3'

services:
  db:
    image: comp-mysql
    expose:
      - 3306
  api:
    build:
      context: ./www/api
      dockerfile: api.docker
    volumes:
      - ./www/api/:/api
    privileged: true
    ports:
      - "8000:8000"
    depends_on:
      - db
